# Scriblark native export input follow-up

Run 34695588044 (public source 53591bdbb0d98b9c11ada181d9cfaed45972be1b) passed the native build, all 135 product tests, package fixtures, installation, template persistence and the first two-page PDF export. The first PDF is 24,219 bytes, SHA-256 ab5eaca2498e85ee24c0e5372786294dbd08d2a3d27f8f4a1650f17ce82406a6. The reopened PDF is visible in the same retained process 4400/main HWND 328022. The second export never opened its dialog.

The new original before/after observations distinguish the failed boundary: first Alt+F opened owned gdkWindowTemp 197008, direct owner 328022. Reopened Alt+F left only the original editor window; no menu appeared. All recorded modifier states were released, the retained foreground/focus/PID remained owned, and both menu screenshots were reviewed. These facts do not identify why SendKeys did not open the second menu. No product runtime defect is asserted or patched.

The bounded driver change emits only the two source-verified File/Export mnemonics through native SendInput, reacquiring the exact live owned foreground/focus before each call. Alt/F down/up is one four-event insertion; Export is one two-event insertion. Existing held modifiers are refused. A missing, ambiguous, foreign, disabled or differently titled GTK menu prevents sending Export into the editor. No refocus, key-down retry or alternate export route is added. On incomplete insertion only unmatched inserted key-downs receive cleanup key-ups, and the original action fails.

The original 27 consumer actions, PDF/note/source/raster/report predicates, lifecycle and cleanup checks remain. Native event counts are supplemental observed metadata; the independent Store verifier requires exact typed four/two insertion counts for both export pairs. They do not establish that GTK handled the events: both dialogs and full independent exports must still pass.

Original unmodified JSON fixtures preserve the successful first open menu and failed reopened state: first-menu-before-export 1,439 bytes / 7029e8f30be9e92fce9e79b325403312b019ae74dde4ffb53e297d18789de55b; reopened-menu-after-file 1,121 bytes / b87bccc6df3b3a98051c906c50964a6fc1cd1979dd1bd9eb41db756ac83bab18. The native plan regression first failed because the new API was missing. The Store regression reproduced 20 accepted malformed insertion facts before its new gate; all 13 workflow evidence tests now pass. Original sequence/ownership/source tests, eight modifier/popup refusals and both raw Windows observations pass locally.

The Windows-only helper fixture uses an owned WinForms File/Export menu and the exact private production emitter. It requires four/two native insertion results, an actually opened menu, one actual Export callback and released modifiers. Reflection bypasses only the GTK-class guard for that explicitly synthetic fixture; the production method always reacquires and enforces the real GTK state. This native fixture and the actual installed GTK sequence have not run on macOS and require the new Windows run.

Microsoft documents serial insertion and the need to account for existing keyboard state: https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput . This change does not install hooks or modify target process memory.
